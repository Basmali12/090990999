# نظام تحديث التطبيق

- مصدر الإصدار الوحيد: `package.json` → `version` (الحالي 0.1.0).
- Vite يضمّن الإصدار في JavaScript ويولّد `dist/version.json` تلقائيًا عند `npm run build`.
- عنوان الإصدار وملاحظاته الاختيارية في `release-notes.json`؛ تاريخ النشر يُولد وقت البناء.
- عند النشر ارفع كامل dist مع version.json. لم تتم إضافة خدمة أو Service Worker.
- الفحص عند التشغيل وfocus وvisibilitychange (بفاصل أدنى 30 ثانية)، وكل 15 دقيقة أثناء الظهور. فشل الشبكة أو JSON لا يوقف التطبيق.
- تحديث HTML بطلب cache: reload ثم الانتقال إلى نفس المسار مع `_appUpdate` جديد. ملفات JS/CSS تحمل أسماء hash من Vite؛ ملف version يُطلب بـ no-store ومع cache buster.
- لا توجد عمليات حذف على LocalStorage أو SessionStorage أو IndexedDB أو CacheStorage. إعادة التحميل بطبيعتها تفقد الحالة الموجودة في ذاكرة React والمدخلات غير المحفوظة.

## اختبار محلي دون نشر

1. `npm run build` ثم `npm run preview -- --port 5181`.
2. افتح `http://127.0.0.1:5181/090990999/`؛ عند تطابق الإصدار لا يظهر تنبيه.
3. لا تغيّر package.json للاختبار: عدّل **dist/version.json فقط** إلى رقم أحدث (مثل 0.1.1 مقابل البناء الحالي 0.1.0)، ويمكنك إضافة notes.
4. أعد تحميل صفحة المعاينة أو ارجع إليها بعد 30 ثانية. افتح التنبيه، واختبر «لاحقًا»؛ يبقى التنبيه متوفرًا.
5. للاختبار الآلي المنطقي، المقارنة تدعم 1.0.1 > 1.0.0 و1.10.0 > 1.2.0 وSemVer prerelease/build metadata، وترفض القيم غير الصالحة.
6. قبل اختبار زر التحديث، أعد dist/version.json إلى الإصدار المضمّن لمحاكاة تطابق الإصدار بعد التحميل. سيختفي التنبيه عند إعادة التحميل؛ تحقّق من بقاء محفظة محلية تجريبية.
7. اختبار الفشل: غيّر محتوى dist/version.json مؤقتًا إلى JSON غير صالح، وأعد تحميل المعاينة. ينبغي ألا يظهر تنبيه وألا يتوقف الموقع.
8. **بعد الاختبار شغّل npm run build مجددًا** لاستعادة الملف الصحيح وإزالة جميع ملفات الاختبار. لا تنشر fixture تجريبية.

لإصدار فعلي لاحق: حدّث version في package.json (وحدّث package-lock باستخدام npm install --package-lock-only)، ثم Build وفحص ونشر معتاد بعد الإذن. المستخدم الذي فتح بناءً أقدم يحصل على تنبيه؛ أول زيارة بعد النشر قد تحمل البناء الجديد مباشرة ولن تحتاج تنبيهًا.
